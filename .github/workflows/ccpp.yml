name: HxECS CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Preparing cmake
      run: |
        mkdir build
        cd build
        cmake ..
    - name: Build
      run: make -j4
